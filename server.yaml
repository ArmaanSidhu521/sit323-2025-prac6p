apiVersion: v1
kind: Service
metadata:
  labels:
    app: node-web
  name: node-web
spec:
  ports:
  - name: mynode
    port: 80
    protocol: TCP
    targetPort: 8080 
    nodePort: 31234
  selector:
    app: node-web
  type: NodePort